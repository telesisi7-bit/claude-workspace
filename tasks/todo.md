# To-Do

작업 항목을 아래 형식으로 추가하세요. 완료되면 `- [x]`로 체크하고 `progress.md`에 기록을 남기세요.

## 진행 중

### 날씨 정보를 디스코드 채널로도 전송 (2026-08-26)

**목표:** 매일 9시 `weather.txt` 저장에 더해, 디스코드 채널에도 예쁜 카드로 알림.

**보안 원칙 (CLAUDE.md 규칙 2·3번)**
- 디스코드 Webhook 주소 = 비밀번호급. 아는 사람은 누구나 채널에 글 쓸 수 있음.
- 코드에 절대 하드코딩 금지 → `.env`에서만 읽음.
- `.env`는 `.gitignore`에 등록. 화면 출력 시 항상 마스킹.

**체크리스트**
- [x] 1. 기존 `weather_fetch.ps1`을 `.bak`으로 백업
- [x] 2. 스크립트에 디스코드 전송 기능 추가 (`.env`에서 주소 읽기)
- [x] 3. `.env.example` 템플릿 + `.gitignore` 생성
- [x] 4. Webhook 주소 없을 때도 안 죽고 파일 저장은 되는지 테스트 — 정상
- [x] 4-1. (추가) 재시도 기능 넣음. 테스트 중 실제로 503을 만나서 필요성 확인됨
- [x] 4-2. 가짜 주소로 전송 경로 검증 — 404 응답 = 카드 생성·전송 정상
- [x] 5. 레오님이 Webhook 만들어 `.env`에 입력 완료
- [x] 6. 실제 전송 테스트 — 수동 실행 성공, 스케줄러 자동실행도 성공
- [x] 7. `progress.md` 기록

**→ 이 작업 완료. 아래 "완료" 항목으로 이동됨.**

**설계**
- 디스코드 embed(색깔 카드) 사용. 미세먼지 등급에 따라 색 변경
  - 좋음=파랑 / 보통=초록 / 나쁨=주황 / 매우나쁨=빨강
- 전송 실패해도 `weather.txt` 저장은 그대로 되게 (알림은 부가기능)

## 대기

- [x] claude-workspace GitHub 프라이빗 저장소 업로드 (2026-08-28 완료)

- [ ] 포트폴리오 「 」 자리표시자 40군데 채우기 (배포 전 필수)
- [ ] Vercel 배포 후 개인문서 404 확인

## 완료

- [x] tasks 폴더 및 기본 문서 구조 생성 (2026-07-17)

### [x] 순천 용당동 날씨/미세먼지 자동 수집 (2026-08-26 완료)

**목표:** 매일 오전 9시에 순천시 용당동 날씨 + 미세먼지를 `weather.txt`에 자동 저장. API키 없이.

**현재 상태 파악 결과**
- `register_task.ps1` (스케줄러 등록용) — 이미 존재
- Windows 작업 스케줄러 `SuncheonWeatherDaily` — 이미 등록됨(Ready), 매일 09:00
- `weather_fetch.ps1` — **없음**. 그래서 매일 9시마다 조용히 실패 중
- → 즉, **`weather_fetch.ps1` 하나만 만들면 완성**

**사용할 서비스 (API키 불필요, 무료)**
- 날씨: `api.open-meteo.com` — 기온/체감/습도/강수/풍속/최고최저
- 미세먼지: `air-quality-api.open-meteo.com` — PM10, PM2.5
- 좌표: 순천시 용당동 위도 34.9629 / 경도 127.5039
- 연결 테스트 완료 (2026-08-26): 날씨 OK, 미세먼지 OK

**체크리스트**
- [x] 1. `weather_fetch.ps1` 작성 (claude-workspace 루트에 새 파일)
- [x] 2. 수동 실행해서 `weather.txt` 생성 확인 — 성공
- [x] 3. 스케줄러 강제 1회 실행 검증 — 결과코드 0, 기록 정상 추가
- [x] 4. `progress.md`에 기록

**안전장치**
- 인터넷/서버 오류 시에도 죽지 않고 오류 내용을 `weather.txt`에 남김
- 개인정보·API키 전송 없음 (좌표만 전송)
- 파일은 덮어쓰지 않고 이어붙이기(append)라 기존 기록이 사라지지 않음

## 2026-08-26 — 레오 포트폴리오 웹페이지
- [x] 1. 새 파일 `portfolio-leo.html` 생성 (기존 portfolio.html 은 건드리지 않음)
- [x] 2. 섹션 구성: 히어로 / 소개 / 경력 3개 / 스킬 / 연락처
- [x] 3. 반응형 + 라이트/다크 모드 대응
- [x] 4. 브라우저에서 실제 열어 동작 확인
- [ ] 5. 경력·연락처 실제 정보는 레오님 확인 후 채우기 (현재 빈칸 placeholder)
